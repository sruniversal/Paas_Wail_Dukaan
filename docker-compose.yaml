services:
  db:
    image: mysql
    restart: always
    ports:
      - "3306:3306" 
      - "33060:33060" 
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: ecommerce_db
    volumes:
      - ./db:/docker-entrypoint-initdb.d
